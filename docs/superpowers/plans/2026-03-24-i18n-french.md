# i18n French Translation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add cookie/header-based i18n to Whiskcam Next.js site with English (default) and French, auto-detecting language from country (FR/BE/CH → fr) with a manual language switcher in the navbar.

**Architecture:** Use `next-intl` v3 in "no URL routing" mode — locale is stored in a `wk-locale` cookie set by the middleware, read by `i18n/request.ts`. All messages are passed to client components via `NextIntlClientProvider` in the root layout. The middleware extends the existing country detection to also set the language cookie on first visit.

**Tech Stack:** Next.js 15 App Router, next-intl v3, TypeScript, `locales/en.json` + `locales/fr.json`

---

### Task 1: Install next-intl and configure next.config.ts

**Files:**
- Modify: `package.json`
- Modify: `next.config.ts`

- [ ] **Step 1: Install next-intl**

```bash
cd "C:/Users/akasw/Documents/IA PLANS/Whiskcam/whiskcam-headless"
pnpm add next-intl
```

- [ ] **Step 2: Wrap next.config.ts with withNextIntl**

Replace the entire `next.config.ts` content:

```typescript
import type { NextConfig } from "next";
import createNextIntlPlugin from "next-intl/plugin";

const withNextIntl = createNextIntlPlugin("./i18n/request.ts");

const config: NextConfig = {
  experimental: {
    ppr: true,
    inlineCss: true,
    useCache: true,
  },
  images: {
    formats: ["image/avif", "image/webp"],
    remotePatterns: [
      {
        protocol: "https",
        hostname: "cdn.shopify.com",
        pathname: "/s/files/**",
      },
    ],
  },
  async redirects() {
    return [
      {
        source: "/password",
        destination: "/",
        permanent: true,
      },
    ];
  },
  async headers() {
    return [
      {
        source: "/(.*)",
        headers: [
          {
            key: "Content-Security-Policy",
            value: [
              "default-src 'self'",
              "script-src 'self' 'unsafe-inline' 'unsafe-eval' https://www.googletagmanager.com https://www.google-analytics.com https://*.clarity.ms https://connect.facebook.net https://analytics.tiktok.com https://*.tiktok.com",
              "style-src 'self' 'unsafe-inline'",
              "img-src 'self' data: blob: https://cdn.shopify.com https://www.google-analytics.com https://www.facebook.com https://*.clarity.ms https://analytics.tiktok.com https://*.tiktok.com",
              "font-src 'self' data:",
              "connect-src 'self' https://*.shopify.com https://*.google-analytics.com https://*.analytics.google.com https://*.clarity.ms https://*.facebook.com https://analytics.tiktok.com https://*.tiktok.com https://*.tiktokapi.eu https://a.klaviyo.com",
              "media-src 'self' blob:",
              "frame-ancestors 'none'",
            ].join("; "),
          },
          { key: "X-Frame-Options", value: "DENY" },
          { key: "X-Content-Type-Options", value: "nosniff" },
          { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
          { key: "Strict-Transport-Security", value: "max-age=63072000; includeSubDomains; preload" },
          { key: "Permissions-Policy", value: "camera=(), microphone=(), geolocation=()" },
        ],
      },
    ];
  },
};

export default withNextIntl(config);
```

- [ ] **Step 3: Commit**

```bash
git add package.json pnpm-lock.yaml next.config.ts
git commit -m "feat(i18n): install next-intl and configure plugin"
```

---

### Task 2: Create i18n/request.ts

**Files:**
- Create: `i18n/request.ts`

- [ ] **Step 1: Create the file**

```typescript
import { getRequestConfig } from "next-intl/server";
import { cookies } from "next/headers";

export default getRequestConfig(async () => {
  const cookieStore = await cookies();
  const locale = cookieStore.get("wk-locale")?.value ?? "en";
  const validLocale = locale === "fr" ? "fr" : "en";

  return {
    locale: validLocale,
    messages: (await import(`../locales/${validLocale}.json`)).default,
  };
});
```

- [ ] **Step 2: Commit**

```bash
git add i18n/request.ts
git commit -m "feat(i18n): add next-intl request config with cookie-based locale"
```

---

### Task 3: Update middleware.ts with locale detection

**Files:**
- Modify: `middleware.ts`

The middleware must:
1. Detect the country (existing logic)
2. Detect the locale: explicit `wk-locale` cookie > country-based (FR/BE/CH/LU/MC → fr) > Accept-Language header > default `en`
3. Set `wk-locale` cookie only if not already set (first visit auto-detection)

- [ ] **Step 1: Replace middleware.ts**

```typescript
import { NextRequest, NextResponse } from "next/server";

const FRENCH_COUNTRIES = new Set([
  "FR", "BE", "CH", "LU", "MC", // Western Europe
  "CI", "SN", "ML", "BF", "GN", "TG", "BJ", "GA", "CG", "CM", "MG", // Francophone Africa
  "CD", "NE", "TD", "CF", "GQ", "RW", "BI", "DJ", "KM", "MU", "SC",
]);

function detectLocale(request: NextRequest, country: string): string {
  // 1. Explicit user preference (cookie already set)
  const explicit = request.cookies.get("wk-locale")?.value;
  if (explicit === "fr" || explicit === "en") return explicit;

  // 2. Country-based detection
  if (FRENCH_COUNTRIES.has(country)) return "fr";

  // 3. Accept-Language header
  const acceptLang = request.headers.get("accept-language") ?? "";
  if (acceptLang.toLowerCase().startsWith("fr")) return "fr";

  // 4. Default
  return "en";
}

export function middleware(request: NextRequest) {
  const country =
    request.headers.get("x-vercel-ip-country") ??
    "FR"; // Default to France (primary market)

  const locale = detectLocale(request, country);

  const response = NextResponse.next();

  // Set country cookie if changed
  const currentCountry = request.cookies.get("country")?.value;
  if (currentCountry !== country) {
    response.cookies.set("country", country, {
      httpOnly: false,
      secure: true,
      sameSite: "lax",
      maxAge: 60 * 60 * 24, // 1 day
    });
  }

  // Set locale cookie if not already set by user (auto-detection only)
  const currentLocale = request.cookies.get("wk-locale")?.value;
  if (!currentLocale) {
    response.cookies.set("wk-locale", locale, {
      httpOnly: false,
      secure: true,
      sameSite: "lax",
      maxAge: 60 * 60 * 24 * 365, // 1 year
    });
  }

  return response;
}

export const config = {
  matcher: [
    "/((?!_next/static|_next/image|favicon.ico|icon.png|apple-icon|manifest.json|images|videos|.*\\.txt$).*)",
  ],
};
```

- [ ] **Step 2: Commit**

```bash
git add middleware.ts
git commit -m "feat(i18n): detect locale from country/Accept-Language, set wk-locale cookie"
```

---

### Task 4: Create locales/en.json

**Files:**
- Create: `locales/en.json`

- [ ] **Step 1: Create the file**

```json
{
  "nav": {
    "shop": "Shop",
    "footage": "Footage",
    "faq": "FAQ",
    "about": "About",
    "openMenu": "Open menu",
    "closeMenu": "Close menu",
    "openCart": "Open cart"
  },
  "announcement": {
    "savePercent": "{pct}% OFF",
    "save": "Save {pct}%",
    "sale": "Sale",
    "plusFreeShipping": "+ Free Shipping",
    "plusFreeShipShort": "+ Free Ship.",
    "ends": "— Ends",
    "staticWithDiscount": "Save {pct}% — Free shipping worldwide",
    "staticDefault": "Free shipping worldwide"
  },
  "hero": {
    "tagline": "Pet Collar Camera",
    "title": "See Their World.",
    "subtitle": "Ever wonder what your cat does when you leave? Now you'll know.",
    "trust": "Loved by 500+ pet parents worldwide",
    "ctaButton": "Get Yours",
    "ctaSubtext": "Free worldwide shipping"
  },
  "stats": {
    "fullHd": "Full HD",
    "wideAngle": "Wide Angle",
    "ultraLight": "Ultra-Light",
    "batteryLife": "Battery Life"
  },
  "videoShowcase": {
    "overline": "Real footage",
    "title": "What Your Cat Actually Does",
    "subtitle": "Unedited clips from cats wearing Whiskcam. Swipe to explore.",
    "pause": "Pause",
    "play": "Play",
    "verifiedOwner": "Verified owner",
    "videos": {
      "0": "The Chase",
      "1": "The Confrontation",
      "2": "The Encounter",
      "3": "Hold My Catnip",
      "4": "Nail Care Session",
      "5": "Cats Gone Wild",
      "6": "Ungrounded"
    },
    "povCaptions": {
      "0": "Garden patrol",
      "1": "Making friends",
      "2": "Rooftop explorer",
      "3": "Neighborhood watch",
      "4": "Sky whiskers",
      "5": "Hold my catnip",
      "6": "Nail care session",
      "7": "Gone wild"
    }
  },
  "features": {
    "overline": "Features",
    "title": "Built for Adventures",
    "subtitle": "Everything your pet needs to become a filmmaker.",
    "items": {
      "0": {
        "title": "1080P Full HD",
        "description": "Crystal-clear footage of every adventure, whisker to tail."
      },
      "1": {
        "title": "170° Wide Angle",
        "description": "Fisheye lens captures everything your pet sees — nothing gets missed."
      },
      "2": {
        "title": "Ultra-Lightweight",
        "description": "At just 26g, your cat won't even notice it's there. Designed for comfort."
      },
      "3": {
        "title": "No App. No WiFi.",
        "description": "Press record. That's it. No complicated setup, no accounts, no hassle."
      },
      "4": {
        "title": "USB-C Charging",
        "description": "Quick charge via USB-C. Full battery in under an hour. Ready to go."
      },
      "5": {
        "title": "Safe for Pets",
        "description": "Clips onto any collar. Compact, smooth edges. Vet-approved design."
      }
    }
  },
  "reviews": {
    "overline": "Reviews",
    "title": "Pet Parents Love It",
    "subtitle": "Real footage. Real reactions.",
    "verifiedBuyer": "Verified Buyer",
    "showPhotos": "Show photos",
    "hidePhotos": "Hide photos",
    "prev": "Previous review",
    "next": "Next review"
  },
  "howItWorks": {
    "overline": "How it works",
    "title": "Simple as 1, 2, 3",
    "subtitle": "No tech skills required. If you can press a button, you can use Whiskcam.",
    "steps": {
      "0": {
        "title": "Clip It On",
        "description": "Attach Whiskcam to your pet's existing collar in seconds. It fits any collar."
      },
      "1": {
        "title": "Press Record",
        "description": "One button. That's all. No app, no WiFi, no setup. Just press and go."
      },
      "2": {
        "title": "Discover Their World",
        "description": "Plug the SD card into your phone with the included adapter. Watch, laugh, share."
      }
    }
  },
  "product": {
    "overline": "Shop",
    "title": "Whiskcam Original",
    "subtitle": "Everything you need to see your pet's secret world.",
    "badgeBestSeller": "Best Seller",
    "badge1080p": "1080P Full HD",
    "badgeNoApp": "No App",
    "reviews": "500+ pet parents",
    "keyBenefits": {
      "0": "1080P Full HD — 170° wide-angle lens",
      "1": "Ultra-light 26g — cats forget it's there",
      "2": "No app, no WiFi — press record & go",
      "3": "90 min battery — USB-C fast charge"
    },
    "freeGiftsLabel": "Free Gifts With Your Order",
    "microsdName": "32GB MicroSD Card",
    "microsdDetail": "Pre-installed, ready to record",
    "adapterName": "USB-C/Lightning Adapter",
    "adapterDetail": "View footage on your phone",
    "shippingName": "Free Worldwide Shipping",
    "shippingDetail": "7-14 business days",
    "free": "FREE",
    "totalValue": "Total value",
    "youPay": "You pay",
    "atcFallback": "Connect Shopify to enable Add to Cart",
    "freeShippingMoneyBack": "Free shipping · 30-day money back",
    "trustShipping": "Free Shipping",
    "trustReturn": "30-Day Return",
    "trustSecure": "Secure Pay",
    "boxContents": {
      "label": "What's in the box",
      "items": {
        "0": { "item": "Whiskcam Camera", "detail": "1080P Full HD with 170° lens" },
        "1": { "item": "32GB MicroSD Card", "detail": "Pre-installed, ready to record" },
        "2": { "item": "SD Card Adapter", "detail": "USB-C/Lightning — view on your phone" },
        "3": { "item": "Adjustable Collar", "detail": "Fits cats & dogs of all sizes" },
        "4": { "item": "USB-C Cable", "detail": "Fast charging, full in under 1 hour" },
        "5": { "item": "Digital Guide", "detail": "\"How to Capture Your Pet's Secret Life\"" }
      }
    }
  },
  "comparison": {
    "overline": "Compare",
    "title": "Why Pet Parents Choose Whiskcam",
    "subtitle": "See how we stack up against other pet cameras.",
    "featureCol": "Feature",
    "othersCol": "Other cameras",
    "footnote": "Action cameras cost €300+. Whiskcam was built for pets — not your wallet.",
    "features": {
      "0": "Price",
      "1": "Resolution",
      "2": "SD Card Included",
      "3": "Phone Adapter Included",
      "4": "No App Required",
      "5": "Free Shipping",
      "6": "30-Day Money Back"
    },
    "others": {
      "1": "720P–1080P"
    }
  },
  "cta": {
    "overline": "Ready?",
    "title": "Ready to See Their World?",
    "subtitle": "Join thousands of pet parents who discovered their pet's secret life.",
    "button": "Shop Now",
    "footnote": "Free shipping worldwide · 30-day money-back guarantee"
  },
  "faq": {
    "overline": "FAQ",
    "title": "Frequently Asked Questions",
    "subtitle": "Got questions? We've got answers.",
    "pageTitle": "Frequently Asked Questions",
    "pageSubtitle": "Everything you need to know about Whiskcam.",
    "items": {
      "0": {
        "question": "What if I'm not satisfied?",
        "answer": "We offer a 30-day money-back guarantee, no questions asked. If you or your pet aren't happy with Whiskcam, simply email us at support@whiskcam.com and we'll process a full refund — you don't even need to return the product. We stand behind every Whiskcam because we know you'll love it."
      },
      "1": {
        "question": "Is Whiskcam safe for my cat?",
        "answer": "Absolutely. At just 26g, Whiskcam is lighter than a small bell. It clips securely onto your pet's existing collar with smooth, rounded edges. Most cats forget it's there within minutes. We recommend using a breakaway collar for outdoor cats, and Whiskcam is best for adult cats over 2kg."
      },
      "2": {
        "question": "How long does the battery last?",
        "answer": "Whiskcam records up to 90 minutes of Full HD video on a single charge. That's perfect for capturing morning explorations and evening patrols — the most exciting moments of your pet's secret life. A full charge takes under an hour via USB-C."
      },
      "3": {
        "question": "Do I need to download an app?",
        "answer": "No app, no WiFi, no accounts. Just press the record button on the camera. When your pet comes home, pop out the SD card, plug it into your phone using the included adapter, and enjoy the footage. It's that simple."
      },
      "4": {
        "question": "How do I watch the videos on my phone?",
        "answer": "Every Whiskcam comes with a MicroSD-to-USB-C/Lightning adapter. Plug it into your phone and view the videos directly in your gallery app. You can also plug the SD card into any computer with a card reader."
      },
      "5": {
        "question": "Will it fit my pet's collar?",
        "answer": "Whiskcam comes with its own adjustable collar, but it also clips onto any existing collar your pet already wears. Works for cats and dogs of all sizes."
      },
      "6": {
        "question": "How long does shipping take?",
        "answer": "We ship worldwide for free. Delivery typically takes 7–14 business days depending on your location. You'll receive a tracking number as soon as your Whiskcam ships."
      },
      "7": {
        "question": "What video format does it record in?",
        "answer": "Whiskcam records in AVI format at 1080P Full HD. The files play on any device — phone, tablet, or computer. You can easily convert or share them on TikTok, Instagram, or YouTube."
      }
    }
  },
  "footer": {
    "tagline": "See their world. The pet collar camera that lets you discover your pet's secret adventures.",
    "shopHeading": "Shop",
    "learnHeading": "Learn",
    "supportHeading": "Support",
    "legalHeading": "Legal",
    "links": {
      "whiskcamOriginal": "Whiskcam Original",
      "whatIsWhiskcam": "What Is Whiskcam?",
      "blogBestCameras": "Best Cat Collar Cameras 2026",
      "blogSafe": "Are Cat Cameras Safe?",
      "allArticles": "All Articles",
      "faq": "FAQ",
      "trackOrder": "Track My Order",
      "shipping": "Shipping",
      "returns": "Returns",
      "contact": "Contact",
      "privacy": "Privacy Policy",
      "terms": "Terms of Service"
    },
    "trustShipping": "Free Worldwide Shipping",
    "trustReturns": "30-Day Money Back",
    "trustSecure": "Secure Checkout",
    "trustPetSafe": "Pet-Safe Design",
    "copyright": "© {year} Whiskcam. All rights reserved."
  },
  "cookie": {
    "message": "We use cookies to improve your experience and analyze site traffic.",
    "privacyLink": "Privacy Policy",
    "decline": "Decline",
    "accept": "Accept"
  },
  "emailPopup": {
    "title": "Get Your Free Pet Photography Guide",
    "subtitle": "Learn the best tips to capture your pet's secret adventures. Join 500+ pet parents.",
    "placeholder": "your@email.com",
    "submit": "Get the Free Guide",
    "dismiss": "No thanks",
    "successTitle": "Thank you!",
    "successSubtitle": "Check your inbox!"
  },
  "cart": {
    "title": "Your Cart",
    "empty": "Your cart is empty.",
    "startShopping": "Start shopping",
    "freeGiftsLabel": "🎁 Free gifts included",
    "freeLabel": "FREE",
    "freeWorldwideShipping": "Free Worldwide Shipping",
    "subtotal": "Subtotal",
    "shipping": "Shipping",
    "total": "Total",
    "checkout": "Complete My Order",
    "processing": "Processing...",
    "secureCheckout": "Secure checkout",
    "moneyBack": "30-day money back",
    "saving": "You're saving {amount} on this order"
  },
  "stickyAtc": {
    "productName": "Whiskcam Original",
    "addToCart": "Yes, I Want This!",
    "adding": "Adding...",
    "added": "Added!"
  },
  "about": {
    "overline": "Our Story",
    "title": "Born from Curiosity",
    "opening": "It started with a simple question: What does my cat actually do all day?",
    "p1": "We'd come home to knocked-over plants, mysterious scratches on the furniture, and a cat that looked suspiciously innocent. We had to know.",
    "p2": "So we strapped a tiny camera to our cat's collar and pressed record. What we saw blew our minds — garden patrols, rooftop adventures, secret meetings with the neighbor's cat. A whole world we never knew existed.",
    "p3": "That's when Whiskcam was born. We wanted every pet parent to experience that same joy — to see the world through their pet's eyes.",
    "p4": "We designed it to be dead simple. No app. No WiFi. No complicated setup. Just clip it on, press record, and let your pet do the rest.",
    "closing": "See their world. You won't believe what you've been missing.",
    "backToShop": "Back to shop"
  },
  "langSwitcher": {
    "label": "Language",
    "en": "EN",
    "fr": "FR"
  }
}
```

---

### Task 5: Create locales/fr.json

**Files:**
- Create: `locales/fr.json`

- [ ] **Step 1: Create the file**

```json
{
  "nav": {
    "shop": "Boutique",
    "footage": "Vidéos",
    "faq": "FAQ",
    "about": "À propos",
    "openMenu": "Ouvrir le menu",
    "closeMenu": "Fermer le menu",
    "openCart": "Ouvrir le panier"
  },
  "announcement": {
    "savePercent": "{pct}% DE RÉDUCTION",
    "save": "Économisez {pct}%",
    "sale": "Soldes",
    "plusFreeShipping": "+ Livraison offerte",
    "plusFreeShipShort": "+ Livraison",
    "ends": "— Se termine",
    "staticWithDiscount": "Économisez {pct}% — Livraison gratuite mondiale",
    "staticDefault": "Livraison gratuite dans le monde entier"
  },
  "hero": {
    "tagline": "Caméra pour collier d'animaux",
    "title": "Voyez leur monde.",
    "subtitle": "Vous vous demandez ce que fait votre chat quand vous partez ? Maintenant vous saurez.",
    "trust": "Adoré par plus de 500 propriétaires d'animaux",
    "ctaButton": "Commander",
    "ctaSubtext": "Livraison gratuite mondiale"
  },
  "stats": {
    "fullHd": "Full HD",
    "wideAngle": "Grand angle",
    "ultraLight": "Ultra-léger",
    "batteryLife": "Autonomie"
  },
  "videoShowcase": {
    "overline": "Vraies vidéos",
    "title": "Ce que votre chat fait vraiment",
    "subtitle": "Clips non montés de chats portant le Whiskcam. Faites défiler pour explorer.",
    "pause": "Pause",
    "play": "Lecture",
    "verifiedOwner": "Propriétaire vérifié",
    "videos": {
      "0": "La Poursuite",
      "1": "La Confrontation",
      "2": "La Rencontre",
      "3": "Mon Catnip, Maintenant",
      "4": "Séance de Griffes",
      "5": "Chats Déchaînés",
      "6": "En Suspension"
    },
    "povCaptions": {
      "0": "Patrouille du jardin",
      "1": "Se faire des amis",
      "2": "Explorateur des toits",
      "3": "Surveillance du quartier",
      "4": "Moustaches dans les étoiles",
      "5": "Mon catnip, maintenant",
      "6": "Séance de griffes",
      "7": "À l'état sauvage"
    }
  },
  "features": {
    "overline": "Fonctionnalités",
    "title": "Conçu pour les aventures",
    "subtitle": "Tout ce dont votre animal a besoin pour devenir réalisateur.",
    "items": {
      "0": {
        "title": "1080P Full HD",
        "description": "Des images cristallines de chaque aventure, des moustaches à la queue."
      },
      "1": {
        "title": "170° Grand angle",
        "description": "L'objectif fisheye capture tout ce que voit votre animal — rien n'échappe."
      },
      "2": {
        "title": "Ultra-léger",
        "description": "À seulement 26g, votre chat ne le remarquera même pas. Conçu pour le confort."
      },
      "3": {
        "title": "Sans appli. Sans WiFi.",
        "description": "Appuyez sur enregistrer. C'est tout. Pas de configuration, pas de compte, pas de prise de tête."
      },
      "4": {
        "title": "Charge USB-C",
        "description": "Charge rapide via USB-C. Batterie complète en moins d'une heure. Prêt à partir."
      },
      "5": {
        "title": "Sûr pour les animaux",
        "description": "Se fixe à n'importe quel collier. Compact, bords lisses. Design approuvé par les vétérinaires."
      }
    }
  },
  "reviews": {
    "overline": "Avis clients",
    "title": "Les propriétaires d'animaux adorent",
    "subtitle": "Vraies vidéos. Vraies réactions.",
    "verifiedBuyer": "Acheteur vérifié",
    "showPhotos": "Voir les photos",
    "hidePhotos": "Masquer les photos",
    "prev": "Avis précédent",
    "next": "Avis suivant"
  },
  "howItWorks": {
    "overline": "Comment ça marche",
    "title": "Simple comme 1, 2, 3",
    "subtitle": "Aucune compétence technique requise. Si vous pouvez appuyer sur un bouton, vous pouvez utiliser Whiskcam.",
    "steps": {
      "0": {
        "title": "Fixez-le",
        "description": "Attachez le Whiskcam au collier de votre animal en quelques secondes. Il s'adapte à n'importe quel collier."
      },
      "1": {
        "title": "Appuyez sur Enregistrer",
        "description": "Un seul bouton. C'est tout. Sans appli, sans WiFi, sans configuration. Appuyez et c'est parti."
      },
      "2": {
        "title": "Découvrez leur monde",
        "description": "Branchez la carte SD sur votre téléphone avec l'adaptateur inclus. Regardez, riez, partagez."
      }
    }
  },
  "product": {
    "overline": "Boutique",
    "title": "Whiskcam Original",
    "subtitle": "Tout ce qu'il faut pour découvrir le monde secret de votre animal.",
    "badgeBestSeller": "Meilleure vente",
    "badge1080p": "1080P Full HD",
    "badgeNoApp": "Sans appli",
    "reviews": "500+ propriétaires d'animaux",
    "keyBenefits": {
      "0": "1080P Full HD — objectif grand angle 170°",
      "1": "Ultra-léger 26g — les chats ne le remarquent pas",
      "2": "Sans appli, sans WiFi — appuyez et enregistrez",
      "3": "90 min de batterie — charge rapide USB-C"
    },
    "freeGiftsLabel": "Cadeaux offerts avec votre commande",
    "microsdName": "Carte MicroSD 32 Go",
    "microsdDetail": "Pré-installée, prête à l'enregistrement",
    "adapterName": "Adaptateur USB-C/Lightning",
    "adapterDetail": "Visionnez vos vidéos sur votre téléphone",
    "shippingName": "Livraison gratuite mondiale",
    "shippingDetail": "7 à 14 jours ouvrés",
    "free": "OFFERT",
    "totalValue": "Valeur totale",
    "youPay": "Vous payez",
    "atcFallback": "Connectez Shopify pour activer l'ajout au panier",
    "freeShippingMoneyBack": "Livraison gratuite · Remboursement 30 jours",
    "trustShipping": "Livraison offerte",
    "trustReturn": "Retour 30 jours",
    "trustSecure": "Paiement sécurisé",
    "boxContents": {
      "label": "Contenu de la boîte",
      "items": {
        "0": { "item": "Caméra Whiskcam", "detail": "1080P Full HD avec objectif 170°" },
        "1": { "item": "Carte MicroSD 32 Go", "detail": "Pré-installée, prête à l'enregistrement" },
        "2": { "item": "Adaptateur carte SD", "detail": "USB-C/Lightning — visionnez sur votre téléphone" },
        "3": { "item": "Collier réglable", "detail": "S'adapte aux chats et chiens de toutes tailles" },
        "4": { "item": "Câble USB-C", "detail": "Charge rapide, complète en moins d'1 heure" },
        "5": { "item": "Guide numérique", "detail": "\"Comment capturer la vie secrète de votre animal\"" }
      }
    }
  },
  "comparison": {
    "overline": "Comparaison",
    "title": "Pourquoi les propriétaires choisissent Whiskcam",
    "subtitle": "Voyez comment nous nous comparons aux autres caméras pour animaux.",
    "featureCol": "Fonctionnalité",
    "othersCol": "Autres caméras",
    "footnote": "Les caméras d'action coûtent 300€+. Whiskcam a été conçu pour les animaux — pas pour votre portefeuille.",
    "features": {
      "0": "Prix",
      "1": "Résolution",
      "2": "Carte SD incluse",
      "3": "Adaptateur téléphone inclus",
      "4": "Sans appli requise",
      "5": "Livraison gratuite",
      "6": "Remboursement 30 jours"
    },
    "others": {
      "1": "720P–1080P"
    }
  },
  "cta": {
    "overline": "Prêt ?",
    "title": "Prêt à voir leur monde ?",
    "subtitle": "Rejoignez des milliers de propriétaires qui ont découvert la vie secrète de leur animal.",
    "button": "Commander maintenant",
    "footnote": "Livraison gratuite mondiale · Remboursement garanti 30 jours"
  },
  "faq": {
    "overline": "FAQ",
    "title": "Questions fréquentes",
    "subtitle": "Des questions ? Nous avons les réponses.",
    "pageTitle": "Questions fréquentes",
    "pageSubtitle": "Tout ce que vous devez savoir sur Whiskcam.",
    "items": {
      "0": {
        "question": "Et si je ne suis pas satisfait ?",
        "answer": "Nous offrons une garantie satisfait ou remboursé de 30 jours, sans poser de questions. Si vous ou votre animal n'êtes pas contents du Whiskcam, envoyez-nous simplement un email à support@whiskcam.com et nous procéderons au remboursement complet — vous n'avez même pas besoin de retourner le produit. Nous sommes fiers de chaque Whiskcam car nous savons que vous l'adorerez."
      },
      "1": {
        "question": "Le Whiskcam est-il sûr pour mon chat ?",
        "answer": "Absolument. À seulement 26g, le Whiskcam est plus léger qu'une petite clochette. Il se fixe solidement au collier existant de votre animal avec des bords lisses et arrondis. La plupart des chats l'oublient en quelques minutes. Nous recommandons d'utiliser un collier détachable pour les chats qui sortent, et le Whiskcam convient aux chats adultes de plus de 2 kg."
      },
      "2": {
        "question": "Quelle est l'autonomie de la batterie ?",
        "answer": "Le Whiskcam enregistre jusqu'à 90 minutes de vidéo Full HD sur une seule charge. C'est parfait pour capturer les explorations matinales et les patrouilles du soir — les moments les plus passionnants de la vie secrète de votre animal. Une charge complète prend moins d'une heure via USB-C."
      },
      "3": {
        "question": "Dois-je télécharger une application ?",
        "answer": "Pas d'application, pas de WiFi, pas de compte. Appuyez simplement sur le bouton d'enregistrement de la caméra. Quand votre animal rentre, retirez la carte SD, branchez-la sur votre téléphone avec l'adaptateur inclus, et profitez des images. C'est aussi simple que ça."
      },
      "4": {
        "question": "Comment voir les vidéos sur mon téléphone ?",
        "answer": "Chaque Whiskcam est livré avec un adaptateur MicroSD vers USB-C/Lightning. Branchez-le sur votre téléphone et visionnez les vidéos directement dans votre galerie photo. Vous pouvez aussi brancher la carte SD sur n'importe quel ordinateur équipé d'un lecteur de carte."
      },
      "5": {
        "question": "S'adapte-t-il au collier de mon animal ?",
        "answer": "Le Whiskcam est livré avec son propre collier réglable, mais il se fixe aussi sur n'importe quel collier existant. Convient aux chats et chiens de toutes tailles."
      },
      "6": {
        "question": "Combien de temps prend la livraison ?",
        "answer": "Nous livrons dans le monde entier gratuitement. La livraison prend généralement 7 à 14 jours ouvrés selon votre localisation. Vous recevrez un numéro de suivi dès que votre Whiskcam sera expédié."
      },
      "7": {
        "question": "Quel format vidéo enregistre-t-il ?",
        "answer": "Le Whiskcam enregistre en format AVI à 1080P Full HD. Les fichiers se lisent sur n'importe quel appareil — téléphone, tablette ou ordinateur. Vous pouvez facilement les convertir ou les partager sur TikTok, Instagram ou YouTube."
      }
    }
  },
  "footer": {
    "tagline": "Voyez leur monde. La caméra pour collier qui vous permet de découvrir les aventures secrètes de votre animal.",
    "shopHeading": "Boutique",
    "learnHeading": "Apprendre",
    "supportHeading": "Assistance",
    "legalHeading": "Mentions légales",
    "links": {
      "whiskcamOriginal": "Whiskcam Original",
      "whatIsWhiskcam": "Qu'est-ce que le Whiskcam ?",
      "blogBestCameras": "Meilleures caméras pour collier 2026",
      "blogSafe": "Les caméras pour chats sont-elles sûres ?",
      "allArticles": "Tous les articles",
      "faq": "FAQ",
      "trackOrder": "Suivre ma commande",
      "shipping": "Livraison",
      "returns": "Retours",
      "contact": "Contact",
      "privacy": "Politique de confidentialité",
      "terms": "Conditions d'utilisation"
    },
    "trustShipping": "Livraison mondiale gratuite",
    "trustReturns": "Remboursement 30 jours",
    "trustSecure": "Paiement sécurisé",
    "trustPetSafe": "Sûr pour les animaux",
    "copyright": "© {year} Whiskcam. Tous droits réservés."
  },
  "cookie": {
    "message": "Nous utilisons des cookies pour améliorer votre expérience et analyser le trafic du site.",
    "privacyLink": "Politique de confidentialité",
    "decline": "Refuser",
    "accept": "Accepter"
  },
  "emailPopup": {
    "title": "Obtenez votre guide photo gratuit",
    "subtitle": "Découvrez les meilleures astuces pour capturer les aventures secrètes de votre animal. Rejoignez 500+ propriétaires.",
    "placeholder": "votre@email.com",
    "submit": "Obtenir le guide gratuit",
    "dismiss": "Non merci",
    "successTitle": "Merci !",
    "successSubtitle": "Vérifiez votre boîte mail !"
  },
  "cart": {
    "title": "Votre panier",
    "empty": "Votre panier est vide.",
    "startShopping": "Commencer vos achats",
    "freeGiftsLabel": "🎁 Cadeaux inclus",
    "freeLabel": "OFFERT",
    "freeWorldwideShipping": "Livraison gratuite mondiale",
    "subtotal": "Sous-total",
    "shipping": "Livraison",
    "total": "Total",
    "checkout": "Finaliser ma commande",
    "processing": "Traitement en cours...",
    "secureCheckout": "Paiement sécurisé",
    "moneyBack": "Remboursement 30 jours",
    "saving": "Vous économisez {amount} sur cette commande"
  },
  "stickyAtc": {
    "productName": "Whiskcam Original",
    "addToCart": "Je le veux !",
    "adding": "Ajout...",
    "added": "Ajouté !"
  },
  "about": {
    "overline": "Notre histoire",
    "title": "Née de la curiosité",
    "opening": "Tout a commencé par une simple question : Qu'est-ce que mon chat fait vraiment toute la journée ?",
    "p1": "On rentrait chez nous pour trouver des plantes renversées, des égratignures mystérieuses sur les meubles, et un chat qui avait l'air suspicieusement innocent. Il fallait savoir.",
    "p2": "Alors on a fixé une minuscule caméra au collier de notre chat et on a appuyé sur enregistrer. Ce qu'on a vu nous a soufflé — des patrouilles dans le jardin, des aventures sur les toits, des réunions secrètes avec le chat du voisin. Tout un monde qu'on ignorait.",
    "p3": "C'est là qu'est né Whiskcam. Nous voulions que chaque propriétaire d'animal vive cette même joie — voir le monde à travers les yeux de son animal.",
    "p4": "Nous l'avons conçu pour être ultra simple. Pas d'appli. Pas de WiFi. Pas de configuration compliquée. Fixez-le, appuyez sur enregistrer, et laissez votre animal faire le reste.",
    "closing": "Voyez leur monde. Vous n'en reviendrez pas de ce que vous avez manqué.",
    "backToShop": "Retour à la boutique"
  },
  "langSwitcher": {
    "label": "Langue",
    "en": "EN",
    "fr": "FR"
  }
}
```

- [ ] **Step 2: Commit both locale files**

```bash
git add locales/en.json locales/fr.json
git commit -m "feat(i18n): add complete English and French translation files"
```

---

### Task 6: Update app/layout.tsx

**Files:**
- Modify: `app/layout.tsx`

Add `NextIntlClientProvider`, use locale for `<html lang>`, update metadata for i18n.

- [ ] **Step 1: Update layout.tsx**

Replace the entire file with:

```typescript
import { Analytics } from "components/analytics";
import { Analytics as VercelAnalytics } from "@vercel/analytics/next";
import { AnnouncementBar } from "components/layout/announcement-bar";
import { CartProvider } from "components/cart/cart-context";
import { CookieConsent } from "components/cookie-consent";
import { EmailPopup } from "components/email-popup";
import { Navbar } from "components/layout/navbar";
import { getCart, getProduct } from "lib/shopify";
import { computeDiscount, computeSavings } from "lib/format";
import { PRODUCT_HANDLE } from "lib/content";
import { NextIntlClientProvider } from "next-intl";
import { getLocale, getMessages } from "next-intl/server";
import { ReactNode } from "react";
import { Toaster } from "sonner";
import { cookies } from "next/headers";
import "./globals.css";
import { baseUrl } from "lib/utils";
import { DM_Sans } from "next/font/google";

const dmSans = DM_Sans({
  subsets: ["latin"],
  variable: "--font-dm-sans",
  display: "swap",
  weight: ["400", "500", "600", "700"],
});

export const metadata = {
  metadataBase: new URL(baseUrl),
  title: {
    default: "Whiskcam — Pet Collar Camera | See Their World",
    template: "%s | Whiskcam",
  },
  description:
    "The lightweight pet collar camera that captures your cat or dog's secret adventures. 1080P Full HD, no app required. Free worldwide shipping.",
  icons: {
    icon: "/icon.png",
    apple: "/apple-icon.png",
  },
  manifest: "/manifest.json",
  robots: {
    follow: true,
    index: true,
  },
  other: {
    "geo.region": "GLOBAL",
    "geo.placename": "Worldwide",
  },
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 5,
  themeColor: "#1A1A1A",
};

export default async function RootLayout({
  children,
}: {
  children: ReactNode;
}) {
  const locale = await getLocale();
  const messages = await getMessages();

  const country = (await cookies()).get("country")?.value || "FR";
  const cart = getCart();
  const product = await getProduct(PRODUCT_HANDLE, country);
  const compareAt = product?.variants[0]?.compareAtPrice;
  const discount = compareAt
    ? computeDiscount(product!.priceRange.maxVariantPrice.amount, compareAt.amount)
    : 0;
  const savingsPerUnit = compareAt
    ? computeSavings(product!.priceRange.maxVariantPrice.amount, compareAt.amount)
    : 0;
  const currencyCode = product?.priceRange.maxVariantPrice.currencyCode || "EUR";

  return (
    <html lang={locale} className={dmSans.variable}>
      <head>
        <meta name="apple-mobile-web-app-capable" content="yes" />
        <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
      </head>
      <body className="bg-white font-[family-name:var(--font-dm-sans)] text-wk-black antialiased">
        <a href="#main-content" className="sr-only focus:not-sr-only focus:fixed focus:left-4 focus:top-4 focus:z-[100] focus:rounded-lg focus:bg-wk-amber focus:px-4 focus:py-2 focus:text-sm focus:font-semibold focus:text-wk-black">
          Skip to content
        </a>
        <NextIntlClientProvider messages={messages} locale={locale}>
          <CartProvider cartPromise={cart}>
            <AnnouncementBar discount={discount} />
            <Navbar savingsPerUnit={savingsPerUnit} currencyCode={currencyCode} />
            <main id="main-content" className="overflow-x-clip">{children}</main>
            <Toaster closeButton />
            <EmailPopup />
            <CookieConsent />
            <Analytics />
            <VercelAnalytics />
          </CartProvider>
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add app/layout.tsx
git commit -m "feat(i18n): wrap layout with NextIntlClientProvider, set html lang from locale"
```

---

### Task 7: Create LanguageSwitcher component

**Files:**
- Create: `components/ui/language-switcher.tsx`

- [ ] **Step 1: Create the component**

```typescript
"use client";

import { useLocale } from "next-intl";

export function LanguageSwitcher({ variant = "dark" }: { variant?: "dark" | "light" }) {
  const locale = useLocale();

  const switchLocale = (newLocale: string) => {
    if (newLocale === locale) return;
    document.cookie = `wk-locale=${newLocale}; max-age=${365 * 24 * 60 * 60}; path=/; samesite=lax; secure`;
    window.location.reload();
  };

  const baseClass = "text-[11px] font-semibold transition-colors";
  const activeClass = variant === "dark" ? "text-white" : "text-wk-black";
  const inactiveClass = variant === "dark"
    ? "text-white/40 hover:text-white/70"
    : "text-wk-grey-400 hover:text-wk-grey-600";
  const dividerClass = variant === "dark" ? "text-white/20" : "text-wk-grey-300";

  return (
    <div className="flex items-center gap-1" aria-label="Language switcher">
      <button
        onClick={() => switchLocale("en")}
        className={`${baseClass} ${locale === "en" ? activeClass : inactiveClass}`}
        aria-current={locale === "en" ? "true" : undefined}
      >
        EN
      </button>
      <span className={`${dividerClass} text-[10px]`} aria-hidden="true">|</span>
      <button
        onClick={() => switchLocale("fr")}
        className={`${baseClass} ${locale === "fr" ? activeClass : inactiveClass}`}
        aria-current={locale === "fr" ? "true" : undefined}
      >
        FR
      </button>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add components/ui/language-switcher.tsx
git commit -m "feat(i18n): add LanguageSwitcher component with cookie-based locale switching"
```

---

### Task 8: Update Navbar

**Files:**
- Modify: `components/layout/navbar/index.tsx`

- [ ] **Step 1: Replace the file**

```typescript
"use client";

import CartModal from "components/cart/modal";
import { LanguageSwitcher } from "components/ui/language-switcher";
import Image from "next/image";
import Link from "next/link";
import { useEffect, useState } from "react";
import { usePathname, useRouter } from "next/navigation";
import { useTranslations } from "next-intl";
import { Bars3Icon, XMarkIcon } from "@heroicons/react/24/outline";

interface NavbarProps {
  savingsPerUnit?: number;
  currencyCode?: string;
}

export function Navbar({ savingsPerUnit, currencyCode }: NavbarProps) {
  const t = useTranslations("nav");
  const [scrolled, setScrolled] = useState(false);
  const [mobileOpen, setMobileOpen] = useState(false);
  const pathname = usePathname();
  const router = useRouter();

  const NAV_LINKS = [
    { title: t("shop"), path: "#product" },
    { title: t("footage"), path: "#footage" },
    { title: t("faq"), path: "#faq" },
    { title: t("about"), path: "/about" },
  ];

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 50);
    window.addEventListener("scroll", handleScroll, { passive: true });
    return () => window.removeEventListener("scroll", handleScroll);
  }, []);

  useEffect(() => {
    document.body.style.overflow = mobileOpen ? "hidden" : "";
    return () => { document.body.style.overflow = ""; };
  }, [mobileOpen]);

  const handleNavClick = (path: string) => {
    setMobileOpen(false);
    if (path.startsWith("#")) {
      if (pathname !== "/") {
        router.push("/" + path);
      } else {
        const el = document.querySelector(path);
        if (el) {
          el.scrollIntoView({ behavior: "smooth" });
        }
      }
    }
  };

  return (
    <>
      <nav
        className={`fixed left-0 right-0 z-50 transition-all duration-300 ${
          scrolled
            ? "top-0 bg-white/90 shadow-sm backdrop-blur-xl"
            : "top-9 bg-transparent"
        }`}
      >
        <div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-3 lg:px-8">
          {/* Mobile hamburger */}
          <button
            className="md:hidden"
            onClick={() => setMobileOpen(true)}
            aria-label={t("openMenu")}
          >
            <Bars3Icon
              className={`h-6 w-6 transition-colors ${
                scrolled ? "text-wk-black" : "text-white"
              }`}
            />
          </button>

          {/* Logo */}
          <Link href="/" className="flex items-center">
            <Image
              src="/images/logos/whiskcam-logo-icon.webp"
              alt="Whiskcam"
              width={28}
              height={28}
              className="block sm:hidden rounded"
            />
            <Image
              src="/images/logos/whiskcam-logo-wordmark.webp"
              alt="Whiskcam"
              width={140}
              height={32}
              className={`hidden sm:block transition-all ${
                scrolled ? "" : "brightness-0 invert"
              }`}
            />
          </Link>

          {/* Desktop nav */}
          <ul className="hidden items-center gap-8 md:flex">
            {NAV_LINKS.map((link) =>
              link.path.startsWith("#") ? (
                <li key={link.title}>
                  <button
                    onClick={() => handleNavClick(link.path)}
                    className={`text-sm font-medium transition-colors ${
                      scrolled
                        ? "text-wk-grey-600 hover:text-wk-black"
                        : "text-white/70 hover:text-white"
                    }`}
                  >
                    {link.title}
                  </button>
                </li>
              ) : (
                <li key={link.title}>
                  <Link
                    href={link.path}
                    className={`text-sm font-medium transition-colors ${
                      scrolled
                        ? "text-wk-grey-600 hover:text-wk-black"
                        : "text-white/70 hover:text-white"
                    }`}
                  >
                    {link.title}
                  </Link>
                </li>
              )
            )}
          </ul>

          {/* Right side: Language switcher + Cart */}
          <div className="flex items-center gap-3">
            <div className="hidden md:block">
              <LanguageSwitcher variant={scrolled ? "light" : "dark"} />
            </div>
            <div className={scrolled ? "text-wk-black" : "text-white"}>
              <CartModal savingsPerUnit={savingsPerUnit} currencyCode={currencyCode} />
            </div>
          </div>
        </div>
      </nav>

      {/* Mobile menu */}
      {mobileOpen && (
        <div className="fixed inset-0 z-[60] bg-white">
          <div className="flex items-center justify-between px-4 py-3">
            <Link href="/" onClick={() => setMobileOpen(false)}>
              <Image
                src="/images/logos/whiskcam-logo-wordmark.webp"
                alt="Whiskcam"
                width={130}
                height={30}
              />
            </Link>
            <button onClick={() => setMobileOpen(false)} aria-label={t("closeMenu")}>
              <XMarkIcon className="h-6 w-6 text-wk-black" />
            </button>
          </div>
          <nav className="mt-8 px-4">
            {NAV_LINKS.map((link) =>
              link.path.startsWith("#") ? (
                <button
                  key={link.title}
                  onClick={() => handleNavClick(link.path)}
                  className="block w-full border-b border-wk-grey-100 py-4 text-left text-lg font-semibold text-wk-black transition-colors hover:text-wk-amber"
                >
                  {link.title}
                </button>
              ) : (
                <Link
                  key={link.title}
                  href={link.path}
                  onClick={() => setMobileOpen(false)}
                  className="block border-b border-wk-grey-100 py-4 text-lg font-semibold text-wk-black transition-colors hover:text-wk-amber"
                >
                  {link.title}
                </Link>
              )
            )}
            {/* Mobile language switcher */}
            <div className="mt-6 flex items-center gap-2">
              <LanguageSwitcher variant="light" />
            </div>
          </nav>
        </div>
      )}
    </>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add components/layout/navbar/index.tsx
git commit -m "feat(i18n): translate Navbar, add LanguageSwitcher"
```

---

### Task 9: Update AnnouncementBar

**Files:**
- Modify: `components/layout/announcement-bar.tsx`

- [ ] **Step 1: Update the component**

Add `useTranslations` import and replace hardcoded strings with `t()` calls. The structure of `AnnouncementBar` stays the same; only strings change.

Replace the JSX return inside `AnnouncementBar`:

```typescript
"use client";

import { useEffect, useState } from "react";
import { useTranslations } from "next-intl";

const PROMO_COOKIE = "wk_promo_end";
const PROMO_DURATION_MS = 72 * 60 * 60 * 1000;

function fmt(n: number) {
  return n.toString().padStart(2, "0");
}

function getPromoEnd(): number | null {
  try {
    const stored = localStorage.getItem(PROMO_COOKIE);
    if (stored) {
      const ts = parseInt(stored, 10);
      if (!isNaN(ts)) return ts;
    }
    const end = Date.now() + PROMO_DURATION_MS;
    localStorage.setItem(PROMO_COOKIE, String(end));
    return end;
  } catch {
    return null;
  }
}

interface AnnouncementBarProps {
  discount?: number;
}

export function AnnouncementBar({ discount }: AnnouncementBarProps) {
  const t = useTranslations("announcement");
  const [time, setTime] = useState<{ h: string; m: string; s: string } | null>(null);
  const [expired, setExpired] = useState(false);

  useEffect(() => {
    const promoEnd = getPromoEnd();
    if (!promoEnd) return;

    const tick = () => {
      const ms = promoEnd - Date.now();
      if (ms <= 0) {
        setExpired(true);
        return;
      }
      setTime({
        h: fmt(Math.floor(ms / 3600000)),
        m: fmt(Math.floor((ms % 3600000) / 60000)),
        s: fmt(Math.floor((ms % 60000) / 1000)),
      });
    };

    tick();
    const id = setInterval(tick, 1000);
    return () => clearInterval(id);
  }, []);

  const pct = discount && discount > 0 ? discount : null;

  return (
    <div className="z-[60] bg-wk-black">
      <div className="flex items-center justify-center gap-1.5 px-3 py-1.5 sm:gap-2 sm:py-2">
        {expired || !time ? (
          <p className="whitespace-nowrap text-[10px] font-medium text-white/90 sm:text-xs">
            {pct ? (
              <>
                <span className="font-semibold text-wk-amber">{t("save", { pct })}</span>
                {" — "}{t("staticDefault")}
              </>
            ) : (
              <>{t("staticDefault")}</>
            )}
          </p>
        ) : (
          <>
            <p className="whitespace-nowrap text-[10px] font-medium text-white/90 sm:text-xs">
              {pct ? (
                <>
                  <span className="font-semibold text-wk-amber">{t("savePercent", { pct })}</span>
                  <span className="hidden sm:inline"> {t("plusFreeShipping")}</span>
                  <span className="sm:hidden"> {t("plusFreeShipShort")}</span>
                </>
              ) : (
                <>
                  <span className="font-semibold text-wk-amber">{t("sale")}</span>
                  <span className="hidden sm:inline"> {t("plusFreeShipping")}</span>
                  <span className="sm:hidden"> {t("plusFreeShipShort")}</span>
                </>
              )}
              {" "}{t("ends")}
            </p>
            <div className="flex items-center gap-px">
              <span className="rounded bg-white/10 px-1 py-0.5 font-mono text-[10px] font-semibold text-white sm:px-1.5 sm:text-xs">{time.h}</span>
              <span className="text-[10px] font-bold text-white/40">:</span>
              <span className="rounded bg-white/10 px-1 py-0.5 font-mono text-[10px] font-semibold text-white sm:px-1.5 sm:text-xs">{time.m}</span>
              <span className="text-[10px] font-bold text-white/40">:</span>
              <span className="rounded bg-white/10 px-1 py-0.5 font-mono text-[10px] font-semibold text-white sm:px-1.5 sm:text-xs">{time.s}</span>
            </div>
          </>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add components/layout/announcement-bar.tsx
git commit -m "feat(i18n): translate AnnouncementBar"
```

---

### Task 10: Update Footer

**Files:**
- Modify: `components/layout/footer.tsx`

Replace hardcoded strings with `useTranslations("footer")`. Since Footer is a server component (no `"use client"`), use `getTranslations`.

- [ ] **Step 1: Update footer.tsx**

Add at the top: `import { getTranslations } from "next-intl/server";`

Make the component `async` and add `const t = await getTranslations("footer");`

Replace all hardcoded strings with `t()` calls. Key changes:
- Footer heading text from `t("shopHeading")`, `t("learnHeading")`, `t("supportHeading")`, `t("legalHeading")`
- Link titles from `t("links.whiskcamOriginal")`, etc.
- Tagline from `t("tagline")`
- Trust badge labels from `t("trustShipping")`, etc.
- Copyright from `t("copyright", { year: currentYear })`

Full replacement:

```typescript
import { getTranslations } from "next-intl/server";
import Image from "next/image";
import Link from "next/link";

export default async function Footer() {
  const t = await getTranslations("footer");
  const currentYear = new Date().getFullYear();

  const footerLinks = {
    shop: [
      { title: t("links.whiskcamOriginal"), path: "/#product" },
      { title: t("links.whatIsWhiskcam"), path: "/what-is-whiskcam" },
    ],
    learn: [
      { title: t("links.blogBestCameras"), path: "/blog/best-cat-collar-cameras-2026" },
      { title: t("links.blogSafe"), path: "/blog/are-cat-collar-cameras-safe" },
      { title: t("links.allArticles"), path: "/blog" },
    ],
    support: [
      { title: t("links.faq"), path: "/#faq" },
      { title: t("links.trackOrder"), path: "/track" },
      { title: t("links.shipping"), path: "/policies/shipping" },
      { title: t("links.returns"), path: "/policies/returns" },
      { title: t("links.contact"), path: "mailto:support@whiskcam.com" },
    ],
    legal: [
      { title: t("links.privacy"), path: "/policies/privacy" },
      { title: t("links.terms"), path: "/policies/terms" },
    ],
  };

  return (
    <footer className="bg-wk-dark text-white">
      <div className="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8">
        <div className="grid gap-12 md:grid-cols-5">
          {/* Brand */}
          <div>
            <Link href="/">
              <Image
                src="/images/logos/whiskcam-logo-wordmark.webp"
                alt="Whiskcam"
                width={120}
                height={28}
                className="brightness-0 invert"
              />
            </Link>
            <p className="mt-4 text-sm leading-relaxed text-wk-grey-500">
              {t("tagline")}
            </p>
            <div className="mt-6 flex gap-4">
              <a href="https://tiktok.com/@whiskcam" target="_blank" rel="noopener noreferrer" className="text-wk-grey-500 transition-colors hover:text-white" aria-label="TikTok">
                <svg className="h-5 w-5" fill="currentColor" viewBox="0 0 24 24"><path d="M19.59 6.69a4.83 4.83 0 0 1-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 0 1-2.88 2.5 2.89 2.89 0 0 1 0-5.78c.27 0 .54.04.79.1V9.01a6.27 6.27 0 0 0-.79-.05 6.34 6.34 0 0 0 0 12.68 6.34 6.34 0 0 0 6.34-6.34V9.19a8.16 8.16 0 0 0 3.76.92V6.69z" /></svg>
              </a>
              <a href="https://instagram.com/whiskcam" target="_blank" rel="noopener noreferrer" className="text-wk-grey-500 transition-colors hover:text-white" aria-label="Instagram">
                <svg className="h-5 w-5" fill="currentColor" viewBox="0 0 24 24"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0zm0 5.838a6.162 6.162 0 100 12.324 6.162 6.162 0 000-12.324zM12 16a4 4 0 110-8 4 4 0 010 8zm6.406-11.845a1.44 1.44 0 100 2.881 1.44 1.44 0 000-2.881z" /></svg>
              </a>
            </div>
          </div>

          {/* Shop */}
          <div>
            <h3 className="mb-4 text-xs font-semibold uppercase tracking-[0.08em] text-wk-grey-500">{t("shopHeading")}</h3>
            <ul className="space-y-3">
              {footerLinks.shop.map((link) => (
                <li key={link.path}><Link href={link.path} className="text-sm text-wk-grey-400 transition-colors hover:text-white">{link.title}</Link></li>
              ))}
            </ul>
          </div>

          {/* Learn */}
          <div>
            <h3 className="mb-4 text-xs font-semibold uppercase tracking-[0.08em] text-wk-grey-500">{t("learnHeading")}</h3>
            <ul className="space-y-3">
              {footerLinks.learn.map((link) => (
                <li key={link.path}><Link href={link.path} className="text-sm text-wk-grey-400 transition-colors hover:text-white">{link.title}</Link></li>
              ))}
            </ul>
          </div>

          {/* Support */}
          <div>
            <h3 className="mb-4 text-xs font-semibold uppercase tracking-[0.08em] text-wk-grey-500">{t("supportHeading")}</h3>
            <ul className="space-y-3">
              {footerLinks.support.map((link) => (
                <li key={link.path}><Link href={link.path} className="text-sm text-wk-grey-400 transition-colors hover:text-white">{link.title}</Link></li>
              ))}
            </ul>
          </div>

          {/* Legal */}
          <div>
            <h3 className="mb-4 text-xs font-semibold uppercase tracking-[0.08em] text-wk-grey-500">{t("legalHeading")}</h3>
            <ul className="space-y-3">
              {footerLinks.legal.map((link) => (
                <li key={link.path}><Link href={link.path} className="text-sm text-wk-grey-400 transition-colors hover:text-white">{link.title}</Link></li>
              ))}
            </ul>
          </div>
        </div>

        {/* Trust badges */}
        <div className="mt-12 grid grid-cols-2 gap-6 border-t border-white/10 pt-8 md:grid-cols-4">
          {[
            { key: "trustShipping" as const, icon: "M1 3h15v13H1zM16 8h4l3 3v5h-7V8zM5.5 21a2.5 2.5 0 100-5 2.5 2.5 0 000 5zM18.5 21a2.5 2.5 0 100-5 2.5 2.5 0 000 5z" },
            { key: "trustReturns" as const, icon: "M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z" },
            { key: "trustSecure" as const, icon: "M3 11h18v11H3zM7 11V7a5 5 0 0110 0v4" },
            { key: "trustPetSafe" as const, icon: "M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z" },
          ].map((badge) => (
            <div key={badge.key} className="flex flex-col items-center text-center">
              <svg className="mb-2 h-6 w-6 text-wk-grey-500" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={1.5}>
                <path strokeLinecap="round" strokeLinejoin="round" d={badge.icon} />
              </svg>
              <p className="text-[10px] font-semibold uppercase tracking-wider text-wk-grey-500 sm:text-xs">
                {t(badge.key)}
              </p>
            </div>
          ))}
        </div>

        {/* Payment logos + Copyright — keep SVGs as-is, only copyright changes */}
        <div className="mt-8 border-t border-white/10 pt-8 text-center">
          {/* [Payment SVGs unchanged — copy from original footer.tsx] */}
          <p className="mt-4 text-xs text-wk-grey-600">
            {t("copyright", { year: currentYear })}
          </p>
        </div>
      </div>
    </footer>
  );
}
```

NOTE: When implementing, copy the payment SVG block from the original footer.tsx unchanged (lines ~130-200 of the original). Only the copyright line and text strings change.

- [ ] **Step 2: Commit**

```bash
git add components/layout/footer.tsx
git commit -m "feat(i18n): translate Footer"
```

---

### Task 11: Update CookieConsent and EmailPopup

**Files:**
- Modify: `components/cookie-consent.tsx`
- Modify: `components/email-popup.tsx`

- [ ] **Step 1: Update cookie-consent.tsx**

Add `import { useTranslations } from "next-intl";` and `const t = useTranslations("cookie");`

Replace:
- `"We use cookies to improve your experience and analyze site traffic."` → `{t("message")}`
- `"Privacy Policy"` → `{t("privacyLink")}`
- `"Decline"` → `{t("decline")}`
- `"Accept"` → `{t("accept")}`

- [ ] **Step 2: Update email-popup.tsx**

Add `import { useTranslations } from "next-intl";` and `const t = useTranslations("emailPopup");`

Replace:
- `"Get Your Free Pet Photography Guide"` → `{t("title")}`
- `"Learn the best tips..."` → `{t("subtitle")}`
- `placeholder="your@email.com"` → `placeholder={t("placeholder")}`
- `"Get the Free Guide"` → `{t("submit")}`
- `"No thanks"` → `{t("dismiss")}`
- `"Thank you!"` → `{t("successTitle")}`
- `"Check your inbox!"` → `{t("successSubtitle")}`

- [ ] **Step 3: Commit**

```bash
git add components/cookie-consent.tsx components/email-popup.tsx
git commit -m "feat(i18n): translate CookieConsent and EmailPopup"
```

---

### Task 12: Update Landing Sections — Hero, Stats, VideoShowcase

**Files:**
- Modify: `components/landing/hero-section.tsx`
- Modify: `components/landing/social-proof-bar.tsx`
- Modify: `components/landing/video-showcase.tsx`
- Modify: `lib/content.ts`

- [ ] **Step 1: Update lib/content.ts — keep only non-text data**

Keep in `lib/content.ts`:
- `STATS` array with only `value` (keep) and remove text `label` (labels go to translations, keep the values like "1080P", "170°", "26g", "90min")
- `FEATURES` array with only `icon` key
- `HOW_IT_WORKS` array with only `step` number
- `BOX_CONTENTS` remove (fully moved to translations)
- `COMPARISON` keep with boolean/structural data; remove `feature` text
- `HERO_CONTENT` keep only `videoSrc` and `posterSrc`
- `VIDEOS` keep `src`, `duration`, `poster` only; remove `title`
- `POV_IMAGES` keep `src` only; remove `alt`, `caption`
- `FAQ_ITEMS` remove entirely (fully in translations)
- `TRUST_BADGES` remove (in translations)
- Keep `PRODUCT_HANDLE`

Updated `lib/content.ts`:

```typescript
// =============================================================================
// Whiskcam — Non-translatable content data
// Text strings live in locales/en.json and locales/fr.json
// =============================================================================

export const HERO_CONTENT = {
  videoSrc: "/videos/wk-video-encounter.mp4",
  posterSrc: "/images/hero/whiskcam-hero.webp",
};

export const STATS = [
  { value: "1080P" },
  { value: "170°" },
  { value: "26g" },
  { value: "90min" },
];

export const FEATURE_ICONS = ["camera", "eye", "feather", "zap", "usb", "shield"] as const;

export const POV_IMAGES = [
  { src: "/images/pov/wk-pov-1.webp" },
  { src: "/images/pov/wk-pov-2.webp" },
  { src: "/images/pov/wk-pov-3.webp" },
  { src: "/images/pov/wk-pov-4.webp" },
  { src: "/images/pov/wk-pov-sky-whiskers.webp" },
  { src: "/images/pov/wk-pov-6.webp" },
  { src: "/images/pov/wk-pov-7.webp" },
  { src: "/images/pov/wk-pov-8.webp" },
];

export const VIDEOS = [
  { src: "/videos/wk-video-chase.mp4", duration: "0:08", poster: "/images/pov/wk-pov-1.webp" },
  { src: "/videos/wk-video-confrontation.mp4", duration: "0:13", poster: "/images/pov/wk-pov-2.webp" },
  { src: "/videos/wk-video-encounter.mp4", duration: "0:22", poster: "/images/pov/wk-pov-3.webp" },
  { src: "/videos/wk-video-catnip.mp4", duration: "0:15", poster: "/images/pov/wk-pov-6.webp" },
  { src: "/videos/wk-video-nails.mp4", duration: "0:12", poster: "/images/pov/wk-pov-7.webp" },
  { src: "/videos/wk-video-wild.mp4", duration: "0:18", poster: "/images/pov/wk-pov-8.webp" },
  { src: "/videos/wk-video-ungrounded.mp4", duration: "0:14", poster: "/images/pov/wk-pov-sky-whiskers.webp" },
];

export const COMPARISON_VALUES = [
  { whiskcam: "€49.90", others: "€59–99" },
  { whiskcam: "1080P Full HD", others: null }, // others from translation key comparison.others.1
  { whiskcam: true, others: false },
  { whiskcam: true, others: false },
  { whiskcam: true, others: false },
  { whiskcam: true, others: false },
  { whiskcam: true, others: false },
];

export const PRODUCT_HANDLE = "whiskcam-original";
```

- [ ] **Step 2: Update hero-section.tsx**

Replace `HERO_CONTENT` imports that use text with `useTranslations`:

```typescript
import { useTranslations } from "next-intl";
import { HERO_CONTENT } from "lib/content";
// ...
const t = useTranslations("hero");
// Replace:
// HERO_CONTENT.tagline → t("tagline")
// HERO_CONTENT.title → t("title")
// HERO_CONTENT.subtitle → t("subtitle")
// HERO_CONTENT.trust → t("trust")
// "Get Yours — {price}" → `${t("ctaButton")} — ${price}`
// "Free worldwide shipping" → t("ctaSubtext")
```

- [ ] **Step 3: Update social-proof-bar.tsx**

```typescript
import { useTranslations } from "next-intl";
import { STATS } from "lib/content";
// ...
const t = useTranslations("stats");
const statLabels = [t("fullHd"), t("wideAngle"), t("ultraLight"), t("batteryLife")];
// Map STATS with index to get translated labels
```

- [ ] **Step 4: Update video-showcase.tsx**

```typescript
import { useTranslations } from "next-intl";
import { VIDEOS, POV_IMAGES } from "lib/content";
// ...
const t = useTranslations("videoShowcase");
// Replace section heading text with t() calls
// Replace video.title with t(`videos.${index}`)
// Replace pov.caption with t(`povCaptions.${index}`)
// Replace pov.alt with appropriate alt text from translations
```

- [ ] **Step 5: Commit**

```bash
git add lib/content.ts components/landing/hero-section.tsx components/landing/social-proof-bar.tsx components/landing/video-showcase.tsx
git commit -m "feat(i18n): translate Hero, SocialProofBar, VideoShowcase; refactor lib/content.ts"
```

---

### Task 13: Update Landing Sections — Features, HowItWorks, Reviews

**Files:**
- Modify: `components/landing/features-grid.tsx`
- Modify: `components/landing/how-it-works.tsx`
- Modify: `components/landing/reviews-section.tsx`

- [ ] **Step 1: Update features-grid.tsx**

```typescript
import { useTranslations } from "next-intl";
import { FEATURE_ICONS } from "lib/content";
// Remove: import { FEATURES } from "lib/content"
// ...
const t = useTranslations("features");
const features = FEATURE_ICONS.map((icon, i) => ({
  icon,
  title: t(`items.${i}.title`),
  description: t(`items.${i}.description`),
}));
// Replace SectionHeading props with t() calls
// overline={t("overline")} title={t("title")} subtitle={t("subtitle")}
```

- [ ] **Step 2: Update how-it-works.tsx**

```typescript
import { useTranslations } from "next-intl";
// Remove: import { HOW_IT_WORKS } from "lib/content"
// ...
const t = useTranslations("howItWorks");
const steps = [0, 1, 2].map((i) => ({
  step: i + 1,
  title: t(`steps.${i}.title`),
  description: t(`steps.${i}.description`),
}));
// Replace SectionHeading with t() calls
```

- [ ] **Step 3: Update reviews-section.tsx**

The REVIEWS data stays in the component (real customer reviews in English — authenticity is preserved).
Only translate the section headers:

```typescript
import { useTranslations } from "next-intl";
// ...
const t = useTranslations("reviews");
// Replace SectionHeading: overline={t("overline")} title={t("title")} subtitle={t("subtitle")}
// Replace "Verified Buyer" → t("verifiedBuyer")
// Replace "Show photos" / "Hide photos" → t("showPhotos") / t("hidePhotos")
// Replace prev/next aria-labels → t("prev") / t("next")
```

- [ ] **Step 4: Commit**

```bash
git add components/landing/features-grid.tsx components/landing/how-it-works.tsx components/landing/reviews-section.tsx
git commit -m "feat(i18n): translate FeaturesGrid, HowItWorks, ReviewsSection"
```

---

### Task 14: Update Landing Sections — Comparison, CTA, FAQ

**Files:**
- Modify: `components/landing/comparison-table.tsx`
- Modify: `components/landing/cta-banner.tsx`
- Modify: `components/landing/faq-section.tsx`
- Modify: `components/landing/faq-accordion.tsx`

- [ ] **Step 1: Update comparison-table.tsx**

```typescript
import { useTranslations } from "next-intl";
import { COMPARISON_VALUES } from "lib/content";
// ...
const t = useTranslations("comparison");
const rows = COMPARISON_VALUES.map((row, i) => ({
  feature: t(`features.${i}`),
  whiskcam: i === 0 && price ? price : row.whiskcam,
  others: i === 1 ? t("others.1") : row.others,
}));
// Replace SectionHeading text with t() calls
// Replace table headers: "Feature" → t("featureCol"), "Other cameras" → t("othersCol")
// Replace footnote with t("footnote")
```

- [ ] **Step 2: Update cta-banner.tsx**

```typescript
import { useTranslations } from "next-intl";
// ...
const t = useTranslations("cta");
// Replace all hardcoded strings with t() calls
// "Ready?" → t("overline")
// "Ready to See Their World?" → t("title")
// "Join thousands..." → t("subtitle")
// "Shop Now" → t("button")
// "Free shipping worldwide · 30-day money-back guarantee" → t("footnote")
```

- [ ] **Step 3: Update faq-section.tsx**

```typescript
import { useTranslations } from "next-intl";
// ...
const t = useTranslations("faq");
const faqItems = [0, 1, 2, 3, 4, 5, 6, 7].map((i) => ({
  question: t(`items.${i}.question`),
  answer: t(`items.${i}.answer`),
}));
// Remove: import { FAQ_ITEMS } from "lib/content"
// Replace SectionHeading with t() calls
// Pass faqItems to FaqAccordion instead of FAQ_ITEMS
```

- [ ] **Step 4: Update faq-accordion.tsx**

No text changes needed — it receives items as props. Verify interface still works.

- [ ] **Step 5: Commit**

```bash
git add components/landing/comparison-table.tsx components/landing/cta-banner.tsx components/landing/faq-section.tsx
git commit -m "feat(i18n): translate ComparisonTable, CtaBanner, FaqSection"
```

---

### Task 15: Update ProductSection and Cart Components

**Files:**
- Modify: `components/landing/product-section.tsx`
- Modify: `components/cart/modal.tsx`
- Modify: `components/ui/sticky-atc-bar.tsx`

- [ ] **Step 1: Update product-section.tsx**

```typescript
import { useTranslations } from "next-intl";
// ...
const t = useTranslations("product");
// Replace all hardcoded strings. Key replacements:
// SectionHeading: overline={t("overline")} title={t("title")} subtitle={t("subtitle")}
// "Best Seller" → t("badgeBestSeller")
// "1080P Full HD" badge → t("badge1080p")
// "No App" badge → t("badgeNoApp")
// "500+ pet parents" → t("reviews")
// key benefits array → [0,1,2,3].map(i => t(`keyBenefits.${i}`))
// "Free Gifts With Your Order" → t("freeGiftsLabel")
// "32GB MicroSD Card" → t("microsdName"), detail → t("microsdDetail")
// "USB-C/Lightning Adapter" → t("adapterName"), detail → t("adapterDetail")
// "Free Worldwide Shipping" → t("shippingName"), detail → t("shippingDetail")
// "FREE" → t("free")
// "Total value" → t("totalValue")
// "You pay" → t("youPay")
// "Connect Shopify..." → t("atcFallback")
// "Free shipping · 30-day money back" → t("freeShippingMoneyBack")
// trust badges: t("trustShipping"), t("trustReturn"), t("trustSecure")
// Box contents section: use t("boxContents.label") and map 0-5 with t(`boxContents.items.${i}.item`) etc.
```

- [ ] **Step 2: Update cart/modal.tsx**

```typescript
import { useTranslations } from "next-intl";
// In CartModal component:
const t = useTranslations("cart");
// Replace:
// "Your Cart" → t("title")
// "Your cart is empty." → t("empty")
// "Start shopping" → t("startShopping")
// "🎁 Free gifts included" → t("freeGiftsLabel")
// "FREE" → t("freeLabel")
// "Free Worldwide Shipping" → t("freeWorldwideShipping")
// "Subtotal" → t("subtotal")
// "Shipping" → t("shipping")
// "Total" → t("total")
// "Secure checkout" → t("secureCheckout")
// "30-day money back" → t("moneyBack")
// saving text: t("saving", { amount: formatPrice(...) })

// In CheckoutButton: also needs useTranslations
// "Complete My Order" → t("checkout")
// "Processing..." → t("processing")
```

Note: `CheckoutButton` is a sub-component that uses `useFormStatus`. Since it's inside a "use client" file, it can call `useTranslations` directly.

- [ ] **Step 3: Update sticky-atc-bar.tsx**

```typescript
import { useTranslations } from "next-intl";
// ...
const t = useTranslations("stickyAtc");
// "Whiskcam Original" → t("productName")
// "Yes, I Want This!" → t("addToCart")
// "Adding..." → t("adding")
// "Added!" → t("added")
```

- [ ] **Step 4: Commit**

```bash
git add components/landing/product-section.tsx components/cart/modal.tsx components/ui/sticky-atc-bar.tsx
git commit -m "feat(i18n): translate ProductSection, CartModal, StickyAtcBar"
```

---

### Task 16: Update Pages — About, FAQ, What-Is-Whiskcam

**Files:**
- Modify: `app/about/page.tsx`
- Modify: `app/faq/page.tsx`

- [ ] **Step 1: Update app/about/page.tsx**

This is a server component. Use `getTranslations`:

```typescript
import { getTranslations } from "next-intl/server";
// ...
export default async function AboutPage() {
  const t = await getTranslations("about");
  // Replace all inline text with t() calls
  // "Our Story" → t("overline")
  // "Born from Curiosity" → t("title")
  // "It started with a simple question: ..." → t("opening")
  // paragraphs → t("p1"), t("p2"), t("p3"), t("p4")
  // "See their world. You won't believe..." → t("closing")
  // "Back to shop" → t("backToShop")
```

- [ ] **Step 2: Update app/faq/page.tsx**

```typescript
import { getTranslations } from "next-intl/server";
// Remove: import { FAQ_ITEMS } from "lib/content"
// ...
export default async function FaqPage() {
  const t = await getTranslations("faq");
  const faqItems = [0, 1, 2, 3, 4, 5, 6, 7].map((i) => ({
    question: t(`items.${i}.question`),
    answer: t(`items.${i}.answer`),
  }));
  // "Frequently Asked Questions" → t("pageTitle")
  // "Everything you need to know about Whiskcam." → t("pageSubtitle")
  // Pass faqItems to FaqAccordion
```

Note: The `metadata` objects and JSON-LD schemas in these pages stay in English for SEO purposes (search engines index in English by default). We can update them later if needed.

- [ ] **Step 3: Commit**

```bash
git add app/about/page.tsx app/faq/page.tsx
git commit -m "feat(i18n): translate About and FAQ pages"
```

---

### Task 17: Final commit and push

- [ ] **Step 1: Verify build passes**

```bash
cd "C:/Users/akasw/Documents/IA PLANS/Whiskcam/whiskcam-headless"
pnpm build
```

Expected: successful build with no TypeScript errors.

- [ ] **Step 2: Final cleanup commit if needed**

```bash
git add -A
git commit -m "feat(i18n): fix any remaining issues from build"
```

- [ ] **Step 3: Push to remote**

```bash
git push
```

---

## Summary of Changes

| File | Action |
|------|--------|
| `locales/en.json` | CREATE — complete English strings |
| `locales/fr.json` | CREATE — complete French translations |
| `i18n/request.ts` | CREATE — next-intl config |
| `components/ui/language-switcher.tsx` | CREATE — EN/FR toggle |
| `package.json` + `pnpm-lock.yaml` | MODIFY — add next-intl |
| `next.config.ts` | MODIFY — wrap with withNextIntl |
| `middleware.ts` | MODIFY — locale detection logic |
| `app/layout.tsx` | MODIFY — NextIntlClientProvider + html lang |
| `lib/content.ts` | MODIFY — strip text, keep structural data |
| `components/layout/navbar/index.tsx` | MODIFY — translations + LanguageSwitcher |
| `components/layout/announcement-bar.tsx` | MODIFY — translations |
| `components/layout/footer.tsx` | MODIFY — translations (server) |
| `components/cookie-consent.tsx` | MODIFY — translations |
| `components/email-popup.tsx` | MODIFY — translations |
| `components/landing/hero-section.tsx` | MODIFY — translations |
| `components/landing/social-proof-bar.tsx` | MODIFY — translations |
| `components/landing/video-showcase.tsx` | MODIFY — translations |
| `components/landing/features-grid.tsx` | MODIFY — translations |
| `components/landing/how-it-works.tsx` | MODIFY — translations |
| `components/landing/reviews-section.tsx` | MODIFY — section headers |
| `components/landing/comparison-table.tsx` | MODIFY — translations |
| `components/landing/cta-banner.tsx` | MODIFY — translations |
| `components/landing/faq-section.tsx` | MODIFY — translations |
| `components/landing/product-section.tsx` | MODIFY — translations |
| `components/cart/modal.tsx` | MODIFY — translations |
| `components/ui/sticky-atc-bar.tsx` | MODIFY — translations |
| `app/about/page.tsx` | MODIFY — translations (server) |
| `app/faq/page.tsx` | MODIFY — translations (server) |
